(env
 (dev
  (flags
   (:standard -w -unused-var-strict))))

(library
 (name lib)
 (modules
  Ast
  Env
  Config
  Nfa
  NfaCollection
  Ir
  Lowering
  Simpl_alpha
  Overapprox
  Underapprox
  Regex
  Solver
  Debug
  Fe
  FT_SIG
  SimplII
  Me
  Model
  Utils
  Z3core)
 ; The z3 findlib package is optional: the static release build strips z3's
 ; OCaml bindings, so the native unsat-core support degrades to a stub there.
 (libraries
  base
  angstrom
  bitv
  smtml
  unix
  iter
  zarith
  (select
   z3core.ml
   from
   (z3 -> z3core.z3.ml)
   (-> z3core.stub.ml)))
 (inline_tests)
 (preprocess
  (pps
   ppx_deriving.show
   ppx_compare
   ppx_variants_conv
   ppx_inline_test
   ppx_assert
   ppx_expect
   ppx_regexp)))
